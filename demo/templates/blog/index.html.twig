{% extends 'base.html.twig' %}


{% block body %}
    <section class="article">
        {% for article in articles %}
            <div class="container">
                <h2>{{ article.title }}</h2>
                <div class="metadata">{{ article.createdAt | date('d/m/Y') }} à {{ article.createdAt | date('H:i') }} dans la categorie Politique</div>
                <img src="{{ article.image }}" alt="">
                <div class="content">
                    {{ article.content | raw }}
                    <p>At sed omnis ipsa sit fugiat autem explicabo ducimus tempore quae, maiores exercitationem deserunt, voluptatum corporis atque non, aspernatur quod facere aperiam. Ex rerum deserunt nostrum similique quis doloremque? Nesciunt, neque adipisci. Aliquid repellat itaque debitis. Dolorum eligendi numquam saepe? Quia, libero doloremque. Asperiores dolorem ullam ducimus libero reiciendis repellendus.</p>
                    <a href="{{ path('blog_show',{'id':article.id }) }}" class="btn btn-primary">Lire la suite</a>
                </div>
            </div>
        {% endfor %}
    </section>
{% endblock %}
